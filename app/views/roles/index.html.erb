<section class="page-section">
  <div class="page-heading">
    <div>
      <p class="eyebrow">Administración</p>
      <h1>Roles y Permisos</h1>
      <p>Gestiona roles base y permisos dinámicos por módulo.</p>
    </div>
    <%= link_to "Nuevo Rol", new_rol_path, class: "ui-button ui-button-primary" %>
  </div>

  <div class="panel">
    <div class="table-shell">
      <table class="data-table">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Estado</th>
            <th>Usuarios</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% @roles.each do |rol| %>
            <tr>
              <td><%= rol.nombre %></td>
              <td><%= rol.descripcion.presence || "-" %></td>
              <td><%= render "shared/badge", text: rol.activo? ? "Activo" : "Inactivo", tone: rol.activo? ? "success" : "warning" %></td>
              <td><%= rol.usuarios.count %></td>
              <td class="table-actions">
                <%= link_to "Ver", rol_path(rol), class: "table-link" %>
                <%= link_to "Editar", edit_rol_path(rol), class: "table-link" %>
                <%= link_to "Permisos", permisos_rol_path(rol), class: "table-link" %>
                <%= button_to "Eliminar", rol_path(rol), method: :delete, class: "table-link table-link-danger button-link", form_class: "inline-form", data: { turbo_confirm: "¿Deseas eliminar este rol?" } %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</section>
